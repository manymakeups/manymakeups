# Many Makeups

Web de estilismo de Ana Zarza. Código propio (Next.js).

`www.manymakeups.com` sigue apuntando a la web de Devi AI hasta que la nueva esté lista. Esta carpeta es solo el proyecto nuevo; no cambia el DNS de Arsys.

```bash
npm run dev
```

Abre [http://localhost:3000](http://localhost:3000).
